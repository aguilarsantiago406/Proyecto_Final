version: "3.9"
services:
  web:
    build: .
    ports:
      - "8004:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
    env_file:
      - .env
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000